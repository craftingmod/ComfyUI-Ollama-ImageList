{
  "name": "ollama-image-list-frontend",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun run typecheck && vite build --config frontend/vite.config.ts",
    "dev": "bun run typecheck && vite build --watch --config frontend/vite.config.ts",
    "typecheck": "tsc --noEmit -p frontend/tsconfig.json",
    "test": "bun test --preload ./frontend/test/setup.ts frontend/test",
    "test:frontend": "bun test --preload ./frontend/test/setup.ts frontend/test",
    "check:frontend": "bun run typecheck && bun run test:frontend && vite build --config frontend/vite.config.ts"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.1",
    "@types/bun": "^1.3.14",
    "typescript": "~5.9.3",
    "vite": "^8.2.0"
  }
}
